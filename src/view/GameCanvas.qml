import QtQuick 2.15
import QtQuick.Controls 2.15

// ═══════════════════════════════════════════════════════════════════════════
//  游戏画布（备用，当前渲染逻辑直接在 main.qml 中）
//  后续迭代如果需要复杂渲染，扩展此组件
// ═══════════════════════════════════════════════════════════════════════════
Item {
    // 当前直接在 main.qml 中用 PlayerPlane/EnemyPlane/BulletShape 渲染
}
